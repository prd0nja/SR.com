const Media = () => (
	<section className="absolute top-0 left-0 flex h-screen w-screen items-center justify-center bg-black z-50">
		<p className="text-lg text-white/60">No room specified.</p>
	</section>
);

export default Media;
